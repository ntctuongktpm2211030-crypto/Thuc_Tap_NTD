/**
 * Re-export from shared logger location.
 * New code should import from '../../../utils/logger' instead.
 */
export { logger } from '../../../utils/logger';
export type { LogLevel } from '../../../utils/logger';
